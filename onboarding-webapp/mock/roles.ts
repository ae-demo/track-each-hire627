// specs/design/security.json → roles[].name, in that file's order. The
// first entry is who a visitor is with no ?role= on the URL — matching
// security.json's coldStartRole ("New Hire").
export const mockRoles = ["New Hire", "IT Staff", "Facilities Staff", "HR Coordinator"];
